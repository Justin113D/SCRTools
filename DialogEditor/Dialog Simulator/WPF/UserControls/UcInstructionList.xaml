<UserControl x:Class="SCR.Tools.Dialog.Simulator.WPF.UserControls.UcInstructionList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SCR.Tools.Dialog.Simulator.WPF.UserControls"
             xmlns:wpf="clr-namespace:SCR.Tools.Dialog.Simulator.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{x:Static wpf:DesignDataFactory.Simulator}">
    <DataGrid 
        ItemsSource="{Binding ExecutedInstructions}" 
        AutoGenerateColumns="False"
        HeadersVisibility="Column"
        BorderThickness="0"
        RowBackground="Transparent"
        Background="Transparent"
        GridLinesVisibility="None">

        <DataGrid.Columns>
            <DataGridTextColumn 
                Header="Instruction"
                Binding="{Binding Instruction}"
                Width="*"/>

            <DataGridTextColumn 
                Header="Old Value"
                Binding="{Binding OldValue}"/>

            <DataGridTextColumn 
                Header="New Value"
                Binding="{Binding NewValue}"/>
        </DataGrid.Columns>
        
    </DataGrid>
</UserControl>
