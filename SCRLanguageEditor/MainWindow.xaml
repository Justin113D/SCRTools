<Window x:Class="SCRLanguageEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SCRLanguageEditor.Viewmodel"
        xmlns:vmBase="clr-namespace:SCRCommon.Viewmodels;assembly=SCRCommon"
        xmlns:base="clr-namespace:SCRCommon.WpfStyles;assembly=SCRCommon"
        xmlns:data="clr-namespace:SCRLanguageEditor.Data"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:VM_Main}"
        Title="SCR Language Editor" Height="500" Width="900"
        MinHeight="200" MinWidth="350"
        Style="{DynamicResource WindowStyle}"
        x:Name="window">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <base:BaseWindowStyle/>

                <ResourceDictionary>
                    <Style TargetType="{x:Type TextBlock}" x:Key="TVI_TextBlock">
                        <Setter Property="TextAlignment" Value="Left"/>
                        <Setter Property="Foreground" Value="{DynamicResource FontColor}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="TextWrapping" Value="Wrap"/>
                    </Style>
                    <Style TargetType="{x:Type TextBox}" x:Key="TVI_TextField">
                        <Setter Property="VerticalAlignment" Value="Top"/>
                        <Setter Property="TextAlignment" Value="Left"/>
                        <Setter Property="Foreground" Value="{DynamicResource FontColor}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="TextWrapping" Value="Wrap"/>
                    </Style>
                    <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="Transparent">
                                        <Path x:Name="ExpandPath"
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="1,1,1,1"
                                            Fill="{DynamicResource FontColor}"
                                            Data="M 4 0 L 8 4 L 4 8 Z"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter Property="Data" TargetName="ExpandPath" Value="M 0 4 L 8 4 L 4 8 Z"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}" x:Key="TVI_Base">
                        <ControlTemplate.Resources>
                            <vmBase:LeftMarginMultiplierConverter Length="20" x:Key="LengthConverter"/>
                        </ControlTemplate.Resources>
                        <StackPanel MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                            <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Margin="0 0 5 0">

                                <Grid Margin="{Binding Converter={StaticResource LengthConverter},
                                    RelativeSource={RelativeSource TemplatedParent}}"
                                      VerticalAlignment="Center">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <ToggleButton x:Name="Expander"
                                        Style="{DynamicResource ExpandCollapseToggleStyle}"
                                        IsChecked="{Binding Path=IsExpanded,
                                        RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"/>
                                    
                                    <ContentPresenter x:Name="contentPresenter" Grid.Column="1" Margin="2 0 0 0"
                                                      HorizontalAlignment="Stretch" ContentSource="Header"/>
                                </Grid>
                            </Border>
                            <Border x:Name="ItemsHost" Padding="0,3,0,3"
                                    BorderThickness="0, 0, 0, 3" BorderBrush="{DynamicResource DisabledBackgroundColor}">
                                <ItemsPresenter/>
                            </Border>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="HasHeader" Value="false"/>
                                    <Condition Property="Height" Value="Auto"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="contentPresenter" Property="MinHeight" Value="19"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SelectedBackgroundColor}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DisabledBackgroundColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                    
                    <Style TargetType="{x:Type TreeViewItem}" x:Key="Node">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="1,0,0,0"/>
                        <Setter Property="Template" Value="{DynamicResource TVI_Base}"/>
                        <Setter Property="KeyboardNavigation.TabNavigation" Value="Continue"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static data:Node+NodeType.ParentNode}">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
                                
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static data:Node+NodeType.StringNode}">
                                <Setter Property="Background" Value="{DynamicResource SecBackgroundColor}"/>
                                
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>

                    <!--Node Templates-->
                    <vmBase:WindowWidthConverter x:Key="WindowWidthConverter">
                        <vmBase:WindowWidthConverter.Percentage>
                            <sys:Single>0.4</sys:Single>
                        </vmBase:WindowWidthConverter.Percentage>
                    </vmBase:WindowWidthConverter>
                    <HierarchicalDataTemplate x:Key="ParentNode" ItemsSource="{Binding Children}">
                        <Grid Margin="0, 4, 0, 4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="{Binding Path=ActualWidth,
                                    RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, 
                                    Converter={StaticResource WindowWidthConverter}}"/>

                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"
                                     Style="{DynamicResource TVI_TextBlock}" FontSize="22"/>
                            <TextBlock Grid.Column="1" Text="{Binding Description}" VerticalAlignment="Center"
                                     Style="{DynamicResource TVI_TextBlock}" FontSize="15" Margin="5"
                                     Foreground="{DynamicResource HintFontColor}" />
                        </Grid>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate x:Key="StringNode" ItemsSource="{x:Null}">
                        <Border BorderThickness="0 1 0 1" BorderBrush="{DynamicResource BackgroundColor}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="{Binding Path=ActualWidth,
                                        RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, 
                                        Converter={StaticResource WindowWidthConverter}}"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" Margin="0,5,0,5"
                                        Style="{DynamicResource TVI_TextBlock}" FontSize="22"/>
                                <Rectangle Grid.Column="1" Fill="{DynamicResource SecBackgroundColorAlt}"/>
                                <TextBox Grid.Column="1" Text="{Binding Value, Mode=TwoWay}" AcceptsReturn="True" Padding="5"
                                        Style="{DynamicResource TVI_TextField}" FontSize="15" VerticalAlignment="Stretch"
                                         />
                                <TextBlock Grid.Column="2" Text="{Binding Description}" Margin="5" VerticalAlignment="Center"
                                        Style="{DynamicResource TVI_TextBlock}" FontSize="15"
                                        Foreground="{DynamicResource HintFontColor}"/>
                            </Grid>
                        </Border>
                    </HierarchicalDataTemplate>
                    <local:NodeTemplateSelector x:Key="TemplateSelector"
                                                ParentTemplate="{StaticResource ParentNode}"
                                                StringTemplate="{StaticResource StringNode}"/>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="New file" Command="{Binding CreateNewFile}"/>
                <MenuItem Header="Open" Command="{Binding LoadFile}"/>
                <MenuItem Header="Import Old"/>
                <Separator/>
                <MenuItem Header="Save"/>
                <MenuItem Header="Save as..."/>
            </MenuItem>
        </Menu>

        <TreeView Grid.Row="1" ItemsSource="{Binding Nodes}"
                  ItemContainerStyle="{DynamicResource Node}" 
                  ItemTemplateSelector="{StaticResource TemplateSelector}" 
                  KeyboardNavigation.TabNavigation="Contained"/>
    </Grid>
</Window>
